vue + java
后台主要用来同级报错信息，是一个 table 主要有查看源码、播放录屏、查看用户行为

1.查看源码
1.1 开发环境
1.1.1 开发环境可以直接拿到报错的文件名："webpack-internal:///./node_modules/.store/babel-loader@8.4.1/node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[0]!./node_modules/.store/vue-loader@15.11.1/node_modules/vue-loader/lib/index.js??vue-loader-options!./src/views/HomeView.vue?vue&type=script&lang=js" (报错是在 vue、jsx 文件报错的)
1.1.2 对文件名解析，拿到报错的真正文件名和文件路径
1.1.2 通过路径+文件名，调监控后台(后端)接口，开发环境直接通过项目 baseUrl+路径+文件名获取到报错的文件，读这个文件，将读取的文件内容返回个监控后台前端。
1.2.3 把对应的错误文件的代码通过 split('\n')进行分割，形成一个数组。
1.2.4 根据报错的行号，找到数组的基于报错的行号前后 5 个索引，将报错代码显示在中间位置。
1.2.5 [].length = 10，意味着展示 10 行代码，根据行号找到对应的报错的行，做高亮展示。

1.2 生产环境
1.2.1 生产环境报错是在 js 文件报错的，根据 js fileName 调用监控后台(后端)的接口（后端会根据 dist 文件里.map 做 fileName 匹配），找到对应的.map 文件。读这个文件，将读取的文件内容返回个监控后台前端。
1.2.2 利用.map 文件，使用第三方包 source-map-js 解析.map 映射文件，根据错误行行号、列号找到对应的原始源代码及报错的位置。
1.2.3 判断是否为第三方包报错，如果是第三方包报错，因为项目引入的是打包后的文件，缺少三方的 map 文件,报错。
1.2.4 根据报错文件的在当前打包后所有文件的索引，知道对应的报错文件的代码的索引，从而找到对应的报错文件的代码。(debugger)
1.2.5 把对应的错误文件的代码通过 split('\n')进行分割，形成一个数组。
1.2.6 根据报错的行号，找到数组的基于报错的行号前后 5 个索引，将报错代码显示在中间位置。
1.2.7 [].length = 10，意味着展示 10 行代码，根据行号找到对应的报错的行，做高亮展示

2.播放录屏
2.1 监控后台(前端)根据 recordScreenId 调 监控后台(后端)接口，拿到对应的录屏 events,并对其使用 unzip 解压(用来解压经过 Base64 编码并经过 gzip 压缩的数据)。
2.2 再通过 rrwebPlayer 播放录屏

3.查看用户行为，查 (sdk 上报给服务端的)用户行为栈(最近 10 条)
